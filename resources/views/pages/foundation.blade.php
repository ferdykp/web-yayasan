@extends('layouts.public')
@section('content')
<main id="main-content" class="mx-auto min-h-[65vh] max-w-5xl px-5 pb-20 pt-32"><a href="/tentang" class="text-sm text-secondary">← Tentang Yayasan</a><h1 class="my-8 font-serif text-5xl text-primary">{{ ['sejarah'=>'Sejarah Yayasan','visi-misi'=>'Visi & Misi','struktur-organisasi'=>'Struktur Organisasi','sambutan'=>'Sambutan Ketua Yayasan'][$section] }}</h1>
@if($section==='sejarah')<p class="whitespace-pre-line text-lg leading-relaxed text-secondary">{{ data_get($item->data,'history') }}</p>@elseif($section==='visi-misi')@foreach(['vision'=>'Visi','mission'=>'Misi','values'=>'Nilai Yayasan'] as $field=>$label)<section class="mb-8 rounded-3xl bg-sage p-8"><h2 class="font-serif text-3xl">{{ $label }}</h2><p class="mt-4 whitespace-pre-line leading-relaxed">{{ data_get($item->data,$field) }}</p></section>@endforeach @elseif($section==='struktur-organisasi')<div class="grid gap-5 md:grid-cols-2">@foreach(data_get($item->data,'organization',[]) as $role=>$name)<div class="rounded-3xl bg-sage p-8"><h2 class="text-xs uppercase tracking-wider text-secondary">{{ $role }}</h2><p class="mt-3 font-serif text-2xl text-primary">{{ $name }}</p></div>@endforeach</div>@else @if(data_get($item->data,'chair_photo'))<img src="{{ \App\Models\Content::mediaUrl($item->data['chair_photo']) }}" alt="{{ data_get($item->data,'chair_name') }}" class="mb-8 h-72 rounded-3xl object-cover">@endif<p class="font-serif text-2xl leading-relaxed">{{ data_get($item->data,'chair_message') }}</p><p class="mt-8 font-bold">{{ data_get($item->data,'chair_name') }}</p><p class="text-secondary">{{ data_get($item->data,'chair_title') }}</p>@endif
</main>
@endsection
